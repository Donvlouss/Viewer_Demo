name: viewer_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.0.0
  bloc: ^8.1.2
  equatable: ^2.0.5
  flutter_bloc: ^8.1.3
  flutter_smart_dialog: ^4.9.2
  sqlite3: ^2.0.0
  sqlite3_flutter_libs: ^0.5.15
  path_provider: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: any
  

flutter:
  uses-material-design: true

  assets:
    - assets\icons8-loading.gif